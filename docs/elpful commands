# Helpful commands

These are some of the commands we used to buld this project

git init
git status
git add .
git commit -m "Commit message"
git checkout -b "New branch"
git push remote branch

(example git pusush origin master)